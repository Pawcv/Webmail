
<div class="list-group">
    @foreach (var summary in Model.Headers)
    {
        <a href="#" id="@summary.UniqueId" class="list-group-item">@summary.UniqueId - @summary.Envelope.Subject (@summary.Envelope.From)</a>
    }
</div>
